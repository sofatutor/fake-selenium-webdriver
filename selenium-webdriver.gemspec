Gem::Specification.new do |s|
  s.name        = "selenium-webdriver"
  s.version     = "0.0.3"
  s.authors     = ["sofatutor"]
  s.description = "This is a fake gem to avoid installing selenium-webdriver."
  s.summary     = "Like selenium-webdriver, but empty."
  
  s.files         = `git ls-files`.split("\n")
  s.require_paths = ["lib"]
end
